#pragma once

// Tracing pins
#define ALERT_OUTPUT_PIN   PinDef(B, 0)
#define DEBUG_REACTOR_IDLE PinDef(A, 2)
#define DEBUG_REACTOR_BUSY PinDef(A, 3)

// Define the pin to blink
#define MY_LED             PinDef(B, 1)
